# Homebrew tap for mimiio

## How do I install?
`brew install asari-fd/mimiio`

Or `brew tap asari-fd/mimiio` and then `brew install mimiio`.

## Documentation
`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).

